{% extends 'base.html.twig' %}

{% block title %}Picture{% endblock %}

{% block body %}
    <h1>Picture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ picture.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ picture.createdAt ? picture.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Caption</th>
                <td>{{ picture.caption }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ picture.fileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('picture_index') }}">back to list</a>

    <a href="{{ path('picture_edit', {'id': picture.id}) }}">edit</a>

    {{ include('picture/_delete_form.html.twig') }}
{% endblock %}
